name: Free Windows RDP

on: workflow_dispatch

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 360
    steps:
      - name: Enable RDP Access
        run: |
          Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server" -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Set-LocalUser -Name "runneradmin" -Password (ConvertTo-SecureString "RDPpassword123" -AsPlainText -Force)
      - name: Show Connection Details
        run: |
          ipconfig
          Write-Host "====================="
          Write-Host "Username: runneradmin"
          Write-Host "Password: RDPpassword123"
          Write-Host "====================="
          Write-Host "Connect using Remote Desktop (mstsc) on your PC."
      - name: Keep Alive
        run: |
          Start-Sleep -Seconds 21600
